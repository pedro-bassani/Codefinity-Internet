#include <stdio.h>

// You may use `%d` specifier

int main()
{
    int* pInt;
    double* pDouble;

    printf("Size of `int*` = %d, size of `double*` = %d\n", sizeof(pInt), sizeof(pDouble));

    return 0;
}